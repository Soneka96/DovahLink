/// A decoded JSON object. Deliberately duplicated from
/// `app/lib/features/connection/data/models/json_map.dart` rather than imported: this SDK is
/// self-contained and independent of the official Flutter app, which depends on it, not the
/// reverse.
typedef JsonMap = Map<String, dynamic>;
